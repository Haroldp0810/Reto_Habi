import React from 'react'

import {
  HeroContainer,
  HeroContent,
  HeroItems,
  HeroH1,
  HeroP,
  HeroBtn
} from './RegistrerElements';

const Registrer = () => {
    return (
      <HeroContainer>
        <HeroContent>
          <HeroItems>
            <HeroH1>Informacion Personal</HeroH1>
          </HeroItems>
        </HeroContent>
      </HeroContainer>
    );
}

export default Registrer